require 'rails_helper'
require 'api'

describe 'asdf' do
  context 'when creating a new todo' do
    # let(:todo_params) { { name: 'clean', info: 'room' } }

    it 'creates a new todo' do
      expect(true).to be true
    end
  end
end
